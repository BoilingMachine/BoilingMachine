/*
var transformY = 0;

$(document).scroll((e) => {
	
	var computerStart = $('#img-pc-without-table').offset();
	var comStartPosition = [computerStart.top, computerStart.left];
	
	var offset = $('#table-scroll').offset();
	
	if( comStartPosition[0] > offset.top){
		return;
	}else{
		transformY++;
		console.log(transformY);
		var transform = "translate(0," + transformY + "px)";
		$('#img-pc-without-table').css('transform,' + transform + ')');
	}
	 	


	
});
*/

